import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tte-grocery-list',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
